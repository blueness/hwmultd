ACLOCAL_AMFLAGS = -I m4

confdir = $(sysconfdir)/hwmultd

MAINCONF = hwmultd.conf
CLIENTCONF = example_cl.conf script_cl.conf entropy_cl.conf
SERVERCONF = example_hw.conf script_hw.conf entropy_hw.conf temp_hw.conf
dist_conf_DATA = $(MAINCONF) $(CLIENTCONF) $(SERVERCONF)

SUBDIRS = scripts
